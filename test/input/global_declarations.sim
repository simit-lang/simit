%%% empty-struct
struct Empty
end

%%% springs
struct Point
	x : Tensor[3](float);
	v : Tensor[3](float);
end

struct Spring
	m  : float;
	l0 : float;
end

extern points Point{};
extern springs Spring[points,points]{};

%%% constants 
const grav      : Tensor[3](float) = [0.0, 0.0, -9.81];
const stiffness : float = 1e3;
const h1        : float = 0.01;
const h2        : float = [0.01];
