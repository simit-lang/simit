element Vertex
  a : int;
end

element Edge
end

extern V : set{Vertex};
extern E : set{Edge}(V);

func asm(e : Edge, v : (Vertex*1)) -> (R : vector[V](int))
  R(v(0)) = 1;
end

export func main()
  V.a = map asm to E reduce +;
end
